{% if product.available and settings.display_quantity_dropdown %}
  <div class="product-quantity js-product-quantity">
    {% if multibuy_prompt and settings.multibuy_enable %}
      <div class="quantity_top_space">&nbsp;</div>
	{% endif %}
    <div class="product-quantity__item">
      <input type="number" name="quantity" data-no-replace value="{{ quantity }}" min="1" max="99" class="input input--primary product-quantity__input js-quantity-input" id="quantity_input">
      <div class="qty-btns">
        <span class="product-quantity__action product-quantity__action--add js-quantity-add" id="quantity_add"></span>
        <span class="product-quantity__action product-quantity__action--remove js-quantity-remove"  id="quantity_remove"></span>
      </div>
    </div>
  </div>
{% else %}
  <input type="hidden" class="js-quantity-input" name="quantity" value="{{ quantity }}" min="1" max="99">
{% endif %}


